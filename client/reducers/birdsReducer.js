/**
 * ************************************
 *
 * @module  birdsReducer
 * @author
 * @date
 * @description reducer for bird data
 *
 * ************************************
 */

import * as types from '../constants/actionTypes';

const initialState = {
  
};

const birdsReducer = (state = initialState, action) => {
    return initialState
};

export default birdsReducer;
